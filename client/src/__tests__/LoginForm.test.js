/*import React from 'react';
import {  render, fireEvent, screen } from '@testing-library/react';

import { LoginForm1} from "../Components/LoginForm";

const testClients = [{ client_id: 2, budget:0, name: "Atabay", surname: "Heydarli", gender: 1, birthday: "1999-01-15", country: "Italy",
region: "Piemonte",address: "Via Gambasca 19",city: "Torino"}];
test('Renders Loginpage correctly', () => {
 const renderResult = render(<LoginForm1  
             clients={testClients}/>);
   expect(renderResult.getByText("Email")).toBeInTheDocument();
    expect(renderResult.getByText("Password")).toBeInTheDocument();
    expect(renderResult.getByText("Login")).toBeInTheDocument();

fireEvent.click(screen.getByText('Login'));
});*/
